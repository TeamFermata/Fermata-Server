{
  "name": "fermata",
  "version": "1.0.0",
  "description": "",
  "main": "./dest/app.js",
  "scripts": {
    "install": "npm install",
    "start": "tsc && node ./dest/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "TracetogetherKorea",
  "license": "ISC",
  "dependencies": {
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.3",
    "@types/mysql": "^2.15.9"
  }
}
